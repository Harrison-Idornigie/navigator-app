APP_NAME=Navigator
APP_IDENTIFIER=io.fleetbase.navigator
APP_LINK_PREFIX=flbnavigator
FLEETBASE_HOST=https://api.fleetbase.io
FLEETBASE_KEY=

# ETA Routing Configuration
# OSRM routing is FREE and used by default (no API key needed)
# Only add Google Maps API key if you need traffic-aware routing
GOOGLE_MAPS_API_KEY=

DEFAULT_COORDINATES=1.369,103.8864
